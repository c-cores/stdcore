#pragma once

#include <std/stream.h>
#include <std/string.h>

namespace core
{

extern flush_t flush;
extern flush_t endl;
extern stream<string> cin;
extern stream<string> cout;
extern stream<string> cerr;

}

